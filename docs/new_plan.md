# 新的开发计划

原本的设计当中，把整个做成一个大系统统一开发。这个没用。是真的没有用。一旦要权衡整个操作系统，就必然会进行取舍，不但起不到408的目的，反而还产生了各种冗余代码，尤其是为调试而生的冗余代码。

包括这个使用opensbi的问题。使用qemu就-bios default就好了，实在没有必要自己再去整一个OpenSBI。

所以新的计划是先单独开发各个操作系统必要的组件，然后再进行统一的开发。

## 更新

各个组件没有使用rv工具链的必要，我考虑直接用host gcc或者用rust都行。
另外。这些组件的实践，我考虑用OSTEP的结构才是符合实际的。
